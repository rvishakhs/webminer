import React from 'react'

function Logo({
    fontSize = "text-2xl",
    iconSize = 20,
} : {
    fontSize?: string;
    iconSize?: number;
}) {
  return (
    <div>
      
    </div>
  )
}

export default Logo
